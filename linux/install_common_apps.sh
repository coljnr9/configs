# Really common stuff
apt install curl net-tools
apt install btop
apt install git

# Rustup + deps
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
apt instlal libopencv-dev clang libclang-dev

# Fira Code font
apt install fonts-firacode

# Cargo installs
cargo install dua exa ripgrep git-graph kondo


